# Future shared packages

Shared, versioned code belongs here only when it has at least two real consumers. The first expected package is a transport protocol definition shared by the browser and authoritative services. It must contain types and validation primitives, not client-side authority over gameplay or competitive results.
