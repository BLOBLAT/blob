{
  "name": "blob",
  "version": "0.1.0",
  "private": true,
  "description": "BLOB — eat, grow, survive.",
  "packageManager": "npm@11.8.0",
  "workspaces": [
    "apps/*",
    "packages/*",
    "services/*"
  ],
  "engines": {
    "node": ">=22.12.0 <23",
    "npm": ">=10"
  },
  "scripts": {
    "build": "npm run build --workspace=@blob/game-server && npm run build --workspace=@blob/web && npm run build --workspace=@blob/platform-api",
    "check": "npm run typecheck && npm run test && npm run build",
    "dev": "concurrently --kill-others-on-fail --names web,server --prefix-colors magenta,cyan \"npm run dev:web\" \"npm run dev:server\"",
    "dev:web": "npm run dev --workspace=@blob/web",
    "dev:server": "npm run dev --workspace=@blob/game-server",
    "dev:platform": "npm run dev --workspace=@blob/platform-api",
    "start": "npm run start --workspace=@blob/game-server",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "npm run typecheck --workspace=@blob/game-core && npm run typecheck --workspace=@blob/protocol && npm run typecheck --workspace=@blob/validation && npm run typecheck --workspace=@blob/shared && npm run typecheck --workspace=@blob/paid-admission-client && npm run typecheck --workspace=@blob/paid-match-runtime && npm run typecheck --workspace=@blob/game-server && npm run typecheck --workspace=@blob/web && npm run typecheck --workspace=@blob/platform-api"
  },
  "devDependencies": {
    "concurrently": "10.0.5",
    "vitest": "4.1.10"
  }
}
